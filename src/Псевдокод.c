/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/

/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/



/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/



/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/




/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/




/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/



/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/





/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/





/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/





/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/




/*======================USB================
// Структура команд ПК <-> программатор
typedef struct {
    uint8_t command;
    uint32_t address;
    uint32_t data;
    uint8_t buffer[256];
    uint16_t length;
} stlink_command_t;

// Базовый набор команд
#define CMD_GET_VERSION    0x01
#define CMD_READ_IDCODE    0x02
#define CMD_READ_MEM32     0x03
#define CMD_WRITE_MEM32    0x04
#define CMD_DEBUG_ENTER    0x05
#define CMD_FLASH_UNLOCK   0x06
======================USB================*/